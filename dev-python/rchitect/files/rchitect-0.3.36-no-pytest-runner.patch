--- a/setup.py
+++ b/setup.py
@@ -44,8 +44,7 @@
     python_requires='>=2.7, !=3.0.*, !=3.1.*, !=3.2.*, !=3.3.*, !=3.4.*',
     install_requires=install_requires,
     setup_requires=[
-        'cffi>=1.10.0',
-        "pytest-runner"
+        'cffi>=1.10.0'
     ],
     extras_require={
         "test": [
